meta {
  name: Reset Password
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "{{resetToken}}",
    "newPassword": "NewPassword123!"
  }
}
