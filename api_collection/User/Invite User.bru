meta {
  name: Invite User
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/users/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "invited.engineer@example.com",
    "role": "Quality Engineer"
  }
}
